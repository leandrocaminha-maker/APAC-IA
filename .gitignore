node_modules/
.env
.env.local
*.log
dist/
desktop.ini
# Transcrições exportadas: conversa de cliente não entra no repositório
data/conversas/
# Backups do prompt gerados por npm run prompt
data/backups/
# Config local do Claude Code (caminhos da máquina, não do projeto)
.claude/
# Capturas de tela caem na raiz ao colar imagem — não são do projeto,
# e costumam mostrar painel administrativo com dado de cliente.
Captura de tela*.png
*.screenshot.png
